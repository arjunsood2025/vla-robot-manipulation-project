# Core deep learning stack
# RTX 5070 / Blackwell:
# Install the CUDA 12.8 builds before installing this file:
#   pip install torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128
torch==2.7.0
torchvision==0.22.0

# Vision-language backbone for the BC baseline
transformers==4.53.3
open-clip-torch==2.30.0
timm==1.0.14

# Robot-learning framework
# Includes SO-101/Feetech support and SmolVLA dependencies.
lerobot[feetech,smolvla]==0.4.1

# Parameter-efficient fine-tuning.
# Must be >=0.17: lerobot 0.4.1's policy package imports the GR00T policy at
# module load, which pulls in diffusers, which hard-fails on older peft. Any
# lerobot-train invocation dies at import time otherwise.
peft==0.20.0
accelerate==1.10.1

# OpenVLA QLoRA only; leave disabled on Windows and the RTX 5070.
# Install separately on a Linux machine with sufficient VRAM if needed:
# bitsandbytes==0.45.2

# Data, configuration, and tracking
numpy==2.2.3
pandas==2.2.3
pyyaml==6.0.2
opencv-python==4.11.0.86
pillow==11.1.0
# NOTE: lerobot 0.4.1 declares wandb<0.22.0, but pre-0.22 wandb enforces a
# strict 40-char API key and rejects the longer prefixed keys W&B now issues.
# Pin above lerobot's ceiling deliberately; wandb.init/log/finish are stable
# across this range so it does not break lerobot's usage. `pip check` will
# report this as a conflict against lerobot's metadata -- that's expected.
wandb==0.28.1
tensorboard==2.19.0
huggingface-hub==0.35.3

# Inference server/client
websockets==14.2
msgpack==1.1.0
msgpack-numpy==0.4.8

# Scientific evaluation
scipy==1.15.2

# Development and testing
pytest==8.3.4
pytest-cov==6.0.0
matplotlib==3.10.3